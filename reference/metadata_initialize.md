# Initialize a metadata data frame

Initialize a metadata data frame

## Usage

``` r
metadata_initialize(filename, id)
```

## Arguments

- filename:

  A file name

- id:

  An id.

## Value

A nested data frame with metadata and the range of labels, na_values and
the na_range itself.
