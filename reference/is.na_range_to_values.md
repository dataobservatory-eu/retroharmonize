# Test whether missing values need harmonization

Checks whether both \`na_values\` and \`na_range\` attributes are
present on a labelled vector.

## Usage

``` r
is.na_range_to_values(x)
```

## Arguments

- x:

  A labelled vector.

## Value

Logical scalar.
